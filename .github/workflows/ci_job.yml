name: Runs tests on project

on:
  push:
    branches-ignore:
      - 'master'

jobs:
  ci-pr-head:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
        env:
          current_branch: ${GITHUB_REF#refs/heads/}
      - name: Reset to last commit made
        run: |
          git reset --hard
      - name: Install packages
        run: |
          npm install
      - name: Run tests
        run: |
          npm test

  ci-pr-merge:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
        env:
          current_branch: ${GITHUB_REF#refs/heads/}
      - name: Rebase with master
        run: |
          git fetch
          git checkout master
          git merge $current_branch
      - name: Install packages
        run: |
          npm install
      - name: Run tests
        run: |
          npm test
